{
  "name": "swayer-starter-app",
  "version": "1.0.0",
  "description": "Initial Swayer application",
  "type": "module",
  "scripts": {
    "start": "swr serve",
    "start:ssr": "swr serve --ssr",
    "build": "swr build && swr render dist/main.js --output dist/index.html --pretty",
    "build:ssr": "swr build && swr render dist/main.js --output dist/index.html --pretty --ssr",
    "build:prod": "swr build --prod && swr render dist/main.js --output dist/index.html --ssr"
  },
  "dependencies": {
    "swayer": "github:rohiievych/swayer#history/alpha"
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "@typescript-eslint/eslint-plugin": "^5.50.0",
    "@typescript-eslint/parser": "^5.50.0",
    "eslint": "^8.33.0",
    "globals": "^13.20.0",
    "typescript": "^4.9.5"
  }
}
